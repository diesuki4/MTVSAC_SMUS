using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// frame(int), active(bool), position(Vector3), rotation(Quaternion)

public class SaveKeyData : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
